
> Stable principles. Vendor-specific notes and links fill the right columns. Add vendors as new columns — the left columns never change.

| Criteria | Sub-criteria | General Principles (Ideal State) | How to Inspect | Redshift | PySpark / Cradle | Athena | Glue |
|---|---|---|---|---|---|---|---|
| **1. Data Modeling & Schema Design** | Schema Pattern | Choose schema based on query patterns. **Star schema** minimizes joins for analytical queries — fact table surrounded by dimension tables. **Snowflake** normalizes dimensions to reduce redundancy at cost of more joins. **One Big Table (OBT)** eliminates joins entirely at the cost of storage and inflexibility. Ideal state: schema matches dominant access pattern, join count is minimized, denormalization is intentional not accidental. | Review query plans for join count and depth; measure p50/p99 query latency across schema options; count joins per query in query log | Star schema recommended; dist and sort keys critical for join performance. [Table design best practices](https://docs.aws.amazon.com/redshift/latest/dg/c_designing-tables-best-practices.html) | Star schema works well; OBT useful when dimension tables are too large to broadcast | Star schema with Parquet + partitioning recommended; schema design directly impacts cost per query | Schema defined and evolved in Glue Catalog; Iceberg/Delta support schema evolution |
| **1. Data Modeling & Schema Design** | Aggregate & Rollup Design | Precompute common aggregations at multiple granularities (hourly → daily → weekly → monthly). Store results in separate, purpose-built tables. Downstream queries hit small aggregated tables instead of scanning large fact tables. Ideal state: repeated aggregation patterns are identified from query logs and eliminated through pre-computation. | Identify repeated aggregation patterns in query logs; measure query latency and bytes scanned before vs after rollups | Materialized views with auto-refresh. `SVV_MV_INFO` for status. [Materialized views](https://docs.aws.amazon.com/redshift/latest/dg/materialized-view-overview.html) | Write pre-aggregated DataFrames as Parquet/Delta; orchestrate via Cradle pipeline dependencies | CTAS to write aggregated tables to S3; query the smaller aggregate table instead of the fact table. [CTAS](https://docs.aws.amazon.com/athena/latest/ug/ctas.html) | Glue ETL jobs to write rollup tables; schedule via Glue Workflows or EventBridge. [Glue Workflows](https://docs.aws.amazon.com/glue/latest/dg/workflows_overview.html) |
| **1. Data Modeling & Schema Design** | Time-Series & Trend Metrics | Design tables with time as a first-class partition column. Pre-compute WoW, MoM, YoY comparisons. Use window functions or pre-joined lagged tables to avoid self-joins on large fact tables. Ideal state: trend queries run on pre-partitioned data with no self-joins, using window functions over sorted partitions. | Check if time-based filters use partition pruning; check EXPLAIN for self-joins on large tables; measure cost of LAG/LEAD vs pre-computed delta columns | Window functions (LAG, LEAD) are efficient; sort key on date column is critical. [Window functions](https://docs.aws.amazon.com/redshift/latest/dg/r_Window_function_synopsis.html) | Window functions available; partition by date for incremental processing. [PySpark Window](https://spark.apache.org/docs/latest/api/python/reference/pyspark.sql/window.html) | Window functions fully supported; partition by date reduces scan cost and cost per query | Incremental processing with job bookmarks prevents full reprocessing of time-series data. [Job bookmarks](https://docs.aws.amazon.com/glue/latest/dg/monitor-continuations.html) |
| **1. Data Modeling & Schema Design** | Slowly Changing Dimensions (SCD) | **Type 1** (overwrite): simplest, loses history. **Type 2** (add row with validity date range): preserves full history, most common for analytics. **Type 3** (add column): tracks only one prior value. Choose based on whether historical point-in-time analysis is needed. Ideal state: dimension changes are captured with correct effective/expiry dates, no overlapping date ranges, and joins use surrogate keys not natural keys. | Verify surrogate keys are used in fact tables; check that validity date ranges don't overlap; measure dimension table size growth over time | Use MERGE or staged INSERT/UPDATE for Type 2 upserts. [MERGE](https://docs.aws.amazon.com/redshift/latest/dg/r_MERGE.html) | Delta Lake MERGE INTO for Type 2. [Delta MERGE](https://docs.delta.io/latest/delta-update.html) | Iceberg MERGE INTO for Type 2 upserts. [Iceberg on Athena](https://docs.aws.amazon.com/athena/latest/ug/querying-iceberg-updating-iceberg-table-data.html) | Native SCD support in Glue ETL. [Glue SCD](https://docs.aws.amazon.com/glue/latest/dg/aws-glue-programming-etl-scd.html) |
| **1. Data Modeling & Schema Design** | High-Cardinality Dimensions (Bridge Tables) | When a dimension has a many-to-many relationship with a fact table (e.g., products with multiple tags, users with multiple roles), use a bridge/junction table instead of arrays or comma-separated values. Exploding arrays at query time on large fact tables causes row multiplication and performance degradation. Ideal state: bridge tables are pre-built at write time; joins produce no unexpected row explosion. | Look for UNNEST/EXPLODE operations on large fact tables in query plans; verify row counts before and after joins don't multiply unexpectedly | Bridge table with dist key matching fact table join key to avoid redistribution on join | Pre-explode high-cardinality arrays into a bridge DataFrame at write time; avoid `explode()` at query time on large datasets | Use `UNNEST` for arrays in ad-hoc queries; pre-exploded bridge tables for repeated queries | Pre-process high-cardinality columns into bridge tables in ETL; avoid runtime explosion |
| **1. Data Modeling & Schema Design** | Access Pattern Alignment | Understand dominant query patterns before finalizing schema. Frequently filtered columns → partition/sort columns. Common join keys → distribution/bucketing keys. Frequently co-read columns → same table (denormalize). Rarely joined → keep normalized. Ideal state: the most expensive queries run with maximum pruning and minimum movement because the schema was designed for them. | Analyze query logs for most frequent WHERE, JOIN ON, GROUP BY columns; measure query latency distribution (p50, p95, p99); check bytes scanned per query | Use `STL_QUERY`, `SVL_QLOG` system tables to analyze historical query patterns. [Query monitoring](https://docs.aws.amazon.com/redshift/latest/dg/r_SVL_QLOG.html) | Spark UI History Server for job-level query pattern analysis | Athena query history + CloudWatch metrics; use workgroups to segment query patterns by team | AWS Glue job metrics in CloudWatch; analyze ETL job scan patterns |
| **2. Physical Storage & Layout** | Storage Format | Use **columnar formats (Parquet, ORC)** for analytical workloads — enable predicate/projection pushdown and compress well. **Row formats (CSV, JSON)** only for operational ingestion. **Open table formats (Iceberg, Delta, Hudi)** add ACID operations, schema evolution, time travel, and incremental processing on top of Parquet/ORC. Ideal state: all analytical data stored in columnar format; open table format used where ACID or incremental reads are needed. | Check file format in storage layer; verify compression is applied; measure bytes scanned with EXPLAIN; compare query cost before vs after format conversion | Internal storage is Redshift-managed columnar. External tables via Redshift Spectrum support Parquet/ORC. [Spectrum formats](https://docs.aws.amazon.com/redshift/latest/dg/c-using-spectrum.html) | Write Parquet by default: `df.write.parquet(...)`. Use Delta for ACID. [Spark Parquet](https://spark.apache.org/docs/latest/sql-data-sources-parquet.html) | Parquet/ORC strongly recommended; bytes scanned directly determines query cost. [Athena columnar storage](https://docs.aws.amazon.com/athena/latest/ug/columnar-storage.html) | Output format configurable; supports Parquet, ORC, Iceberg, Delta. [Glue formats](https://docs.aws.amazon.com/glue/latest/dg/aws-glue-programming-etl-format.html) |
| **2. Physical Storage & Layout** | File Size | Target ~128 MB per file. Very small files (<10 MB) cause excessive metadata reads, task scheduling overhead, and slow query start times. Very large files (>1 GB) reduce parallelism. Ideal state: files are uniformly sized around 128 MB, number of files per partition matches cluster parallelism, and file count grows predictably with data volume. | `aws s3 ls --recursive | awk '{print $3, $4}'` to inspect file sizes; check number of tasks created per partition in Spark UI; count files per partition | Automatic for internal tables. For Redshift Spectrum, compact S3 files to ~128 MB before querying. | `df.repartition(n)` to increase file count; `df.coalesce(n)` to reduce without full shuffle. [Spark repartition](https://spark.apache.org/docs/latest/api/python/reference/pyspark.sql/api/pyspark.sql.DataFrame.repartition.html) | Small files significantly hurt query performance; use Iceberg compaction (`OPTIMIZE`) or periodic rewrite jobs | `DynamicFrame.repartition()` or `groupFiles` in S3 sink to control output file size. [Glue file grouping](https://docs.aws.amazon.com/glue/latest/dg/grouping-input-files.html) |
| **2. Physical Storage & Layout** | Encoding & Compression | Apply column-level encoding to reduce I/O and storage. **Dictionary encoding** for low-cardinality columns; **delta encoding** for sorted numeric/date sequences; **run-length encoding (RLE)** for repetitive values. Don't compress boolean columns or small tables — overhead exceeds benefit. Ideal state: encoding is chosen per-column based on data characteristics, not applied uniformly. | Check storage size before/after encoding; verify compression ratio per column in metadata; measure I/O reduction on queries | `ANALYZE COMPRESSION` recommends encodings per column. Avoid encoding sort keys. [Redshift compression](https://docs.aws.amazon.com/redshift/latest/dg/t_Compressing_data_on_disk.html). Small tables and boolean columns don't benefit from compression. | Parquet handles encoding internally (dictionary, RLE). File-level: Snappy (default), Gzip, Zstd. [Parquet compression](https://spark.apache.org/docs/latest/sql-performance-tuning.html) | Use Parquet with Snappy or Zstd; check `BytesScanned` in query results to validate impact. [Athena compression](https://docs.aws.amazon.com/athena/latest/ug/compression-formats.html) | Set compression in write options: `format_options={"compression": "snappy"}`; Parquet handles column-level encoding automatically |
| **2. Physical Storage & Layout** | Partitioning & Distribution Strategy | Partition on **high-selectivity, commonly filtered columns** (e.g., date, region, marketplace). Distribute/bucket on **join keys** to co-locate matching rows and eliminate shuffle. Avoid over-partitioning (too many tiny partitions) and under-partitioning (no pruning). Ideal state: every query uses partition pruning to eliminate irrelevant data; joins on distributed keys require no data movement. | EXPLAIN plan for partition pruning evidence; check partitions scanned vs total in query stats; measure shuffle bytes in Spark UI for join queries | `DISTKEY` on join column; `SORTKEY` on filter column. [Distribution styles](https://docs.aws.amazon.com/redshift/latest/dg/c_choosing_dist_style.html) | `df.write.partitionBy("date","region")`; bucketing: `df.write.bucketBy(n, "join_key")`. [Spark partitioning](https://spark.apache.org/docs/latest/sql-performance-tuning.html) | Partition by date, region etc.; use partition projection to avoid Glue Catalog overhead. [Athena partitioning](https://docs.aws.amazon.com/athena/latest/ug/partitions.html) | Partition columns defined in Glue Catalog; `enableUpdateCatalog=True` to auto-register new partitions. [Glue partitions](https://docs.aws.amazon.com/glue/latest/dg/aws-glue-programming-etl-partitions.html) |
| **2. Physical Storage & Layout** | Data Types | Use the smallest type that correctly represents the data. INT over BIGINT, CHAR(n) over VARCHAR(MAX), DATE over TIMESTAMP where time component is unused. Mismatched types in joins force implicit casts, which disable partition pruning and index usage. Ideal state: each column uses the minimal type, implicit casts are absent from all execution plans, and storage footprint is minimized. | Review DDL for oversized types; check EXPLAIN for implicit cast warnings; compare storage size with optimal vs current types | Don't use VARCHAR(MAX) unnecessarily. `INT4` vs `INT8` matters for sort/dist key efficiency. [Redshift data types](https://docs.aws.amazon.com/redshift/latest/dg/c_Supported_data_types.html) | Define schema explicitly with `StructType`; avoid `inferSchema=True` in production — it defaults to overly wide types. [Spark schema](https://spark.apache.org/docs/latest/sql-ref-datatypes.html) | Use `DATE` not `STRING` for date columns — string comparisons disable partition pruning. [Athena data types](https://docs.aws.amazon.com/athena/latest/ug/data-types.html) | Schema defined in Glue Catalog; Glue Schema Registry for type enforcement. [Schema Registry](https://docs.aws.amazon.com/glue/latest/dg/schema-registry.html) |
| **2. Physical Storage & Layout** | Clustering Structures & Indexes | Traditional B-tree indexes don't apply in distributed analytical engines. Alternatives: **sort/cluster keys** create min/max zone maps for block skipping; **Z-ordering** optimizes multi-dimensional range queries; **bloom filters** accelerate point lookups. Ideal state: high-frequency filter columns have zone maps or bloom filters that allow the engine to skip the majority of data blocks without reading them. | Verify sort key is used in query plan; measure block skipping effectiveness via scan reduction; check zone map hit rate | `SORTKEY` (compound or interleaved); don't encode the leading sort key; no benefit for tables < 1 MB. [Redshift sort keys](https://docs.aws.amazon.com/redshift/latest/dg/c_best-practices-sort-key.html) | Delta: `OPTIMIZE table ZORDER BY (col1, col2)` for multi-dimensional clustering. [Delta Z-ordering](https://docs.delta.io/latest/optimizations-oss.html) | Iceberg: `OPTIMIZE table REWRITE DATA USING BIN_PACK` with sort order. [Iceberg optimization on Athena](https://docs.aws.amazon.com/athena/latest/ug/querying-iceberg-data-optimization.html) | Iceberg/Delta table optimization via Glue ETL jobs; schedule as periodic maintenance |
| **2. Physical Storage & Layout** | Table Statistics | Query optimizers rely on statistics (row counts, column cardinality, min/max values, histograms) to choose execution plans. Stale or missing statistics cause poor join order selection, wrong join strategies, and bad memory estimates. Ideal state: statistics are current (updated after significant data changes), accurate, and cover all columns used in filters and joins. | Check last statistics update timestamp; compare estimated vs actual row counts in EXPLAIN output; look for > 10x discrepancy as a red flag | `ANALYZE table_name` updates stats; auto-analyze enabled by default. `SVV_TABLE_INFO` for stats freshness. [Redshift ANALYZE](https://docs.aws.amazon.com/redshift/latest/dg/r_ANALYZE.html) | `ANALYZE TABLE t COMPUTE STATISTICS FOR ALL COLUMNS`; statistics stored in metastore. [Spark statistics](https://spark.apache.org/docs/latest/sql-performance-tuning.html#collecting-table-statistics) | Statistics managed by Glue Data Catalog; Iceberg tables maintain built-in column-level statistics. [Athena statistics](https://docs.aws.amazon.com/athena/latest/ug/glue-best-practices.html) | Glue Data Catalog table statistics; trigger stats update after each ETL job. [Glue statistics](https://docs.aws.amazon.com/glue/latest/dg/data-catalog-table-statistics.html) |
| **2. Physical Storage & Layout** | Metadata (Open Table Formats) | Open table formats (Iceberg, Delta, Hudi) maintain a metadata layer: schema, partition map, snapshot/commit history, and column-level statistics. This metadata enables time travel, incremental reads, and accurate query planning. Metadata bloat from uncleaned snapshots degrades query planning and increases storage costs. Ideal state: metadata is compact, snapshots are expired per policy, and column statistics are populated. | Check snapshot count and size; measure metadata file size growth over time; verify partition statistics are populated and used in plans | Internal metadata is managed automatically. Redshift Spectrum reads Iceberg/Delta metadata from S3. [Redshift Spectrum](https://docs.aws.amazon.com/redshift/latest/dg/c-using-spectrum.html) | Delta: `DESCRIBE HISTORY table` for commit log. Iceberg: `SELECT * FROM table.snapshots`. [Delta utility](https://docs.delta.io/latest/delta-utility.html) | Iceberg metadata queries: `SELECT * FROM "db"."table$snapshots"`. Expire with `VACUUM`. [Iceberg metadata on Athena](https://docs.aws.amazon.com/athena/latest/ug/querying-iceberg-table-metadata.html) | Glue Catalog stores table metadata; Iceberg metadata in S3. [Glue Iceberg integration](https://docs.aws.amazon.com/glue/latest/dg/aws-glue-programming-etl-format-iceberg.html) |
| **3. Data Ingestion** | Load Pattern (Bulk / Incremental / CDC) | **Bulk load**: full refresh of target table; simple but expensive for large tables. **Incremental**: append new/changed records using a watermark (timestamp or sequence number); much cheaper than bulk for stable schemas. **CDC (Change Data Capture)**: captures inserts/updates/deletes from source transaction log; most accurate for operational source systems. Ideal state: load pattern matches change frequency — CDC or incremental for large frequently-changing tables, bulk only for small or rarely-changed tables. | Measure load duration vs data volume over time; check for duplicate records after incremental loads; verify no data gaps in watermark sequence | `COPY` for bulk load; `MERGE` for upsert/CDC. [COPY](https://docs.aws.amazon.com/redshift/latest/dg/r_COPY.html) \| [MERGE](https://docs.aws.amazon.com/redshift/latest/dg/r_MERGE.html) | Read from Kinesis/Kafka for streaming CDC; batch incremental with watermark filters. [Structured Streaming](https://spark.apache.org/docs/latest/structured-streaming-programming-guide.html) | Primarily for querying; loading via S3 + Glue Catalog. Iceberg MERGE INTO for CDC. [Iceberg MERGE on Athena](https://docs.aws.amazon.com/athena/latest/ug/querying-iceberg-updating-iceberg-table-data.html) | Native CDC with Glue ETL + AWS DMS. [AWS DMS](https://docs.aws.amazon.com/dms/latest/userguide/Welcome.html) \| [Glue CDC](https://docs.aws.amazon.com/glue/latest/dg/monitor-continuous-logging.html) |
| **3. Data Ingestion** | Source-Specific Considerations | Every source has idiosyncrasies: **schema drift** (columns added/removed/renamed), **late-arriving data**, **encoding inconsistencies**, **API rate limits**, **connector-level pushdown limitations**. Ideal state: ingestion handles each gracefully — schema changes are detected and propagated (not silently dropped), late arrivals have a defined correction window, and source row counts are reconciled against loaded row counts. | Monitor ingestion job failures and row count reconciliation; track schema change events; measure late arrival rate; alert on source-to-target row count mismatch | `JSONPaths` files for semi-structured sources; `ACCEPTINVCHARS` for encoding issues in COPY. [COPY parameters](https://docs.aws.amazon.com/redshift/latest/dg/r_COPY-parameters.html) | `mergeSchema=True` for schema evolution; watermarks in Structured Streaming for late data. [Spark schema merge](https://spark.apache.org/docs/latest/sql-data-sources-parquet.html#schema-merging) | Schema-on-read tolerates drift; Iceberg for strict schema enforcement with evolution. [Athena schema evolution](https://docs.aws.amazon.com/athena/latest/ug/querying-iceberg-schema-evolution.html) | Glue Schema Registry for type enforcement; Crawlers for schema discovery. [Schema Registry](https://docs.aws.amazon.com/glue/latest/dg/schema-registry.html) \| [Crawlers](https://docs.aws.amazon.com/glue/latest/dg/add-crawler.html) |
| **4. Query & Compute Execution** | Data Reduction (Predicate / Partition / Projection Pushdown) | Reduce data read from the storage layer as early as possible. Three levers: **(1) Predicate pushdown** — push WHERE filters to the storage layer so only matching rows are read; **(2) Partition pruning** — eliminate entire partitions using filter columns; **(3) Projection pruning** — read only required columns (especially effective with columnar formats). Avoid applying functions on filtered columns (e.g., `WHERE YEAR(date) = 2024`) — this disables pushdown. Ideal state: bytes scanned equals only what the query actually needs. | EXPLAIN plan for pushdown evidence; check "bytes scanned" metric in query results; verify partition filter application in logs; compare data scanned before and after query rewrites | Zone maps on sort key columns enable block-level skipping. EXPLAIN for scan details. [Redshift query planning](https://docs.aws.amazon.com/redshift/latest/dg/c-the-query-plan.html) | Driver log: `Found N partition filters to apply: EqualsAndesFilter(marketplace_id,1)...` [Spark predicate pushdown](https://spark.apache.org/docs/latest/sql-performance-tuning.html) \| BDT Tech Talks — Tuning and Optimizing Jobs on Cradle | Partition pruning shown in query statistics; bytes scanned visible in results. [Athena partitions best practices](https://docs.aws.amazon.com/athena/latest/ug/partitions.html) | Pushdown predicates via `push_down_predicate` parameter: `glueContext.create_dynamic_frame.from_catalog(push_down_predicate="year='2024'")`. [Glue pushdown](https://docs.aws.amazon.com/glue/latest/dg/aws-glue-programming-etl-partitions.html#aws-glue-programming-etl-partitions-pushdowns) |
| **4. Query & Compute Execution** | Join Strategy | Preferred strategy order: **(1) Broadcast Hash Join (BHJ)** — small table replicated to all nodes, zero shuffle, best option; **(2) Shuffle Hash Join (SHJ)** — data redistributed so matching keys are co-located; **(3) Sort-Merge Join (SMJ)** — used for very large tables, sorts both sides, may spill to disk; **(4) Nested Loop Join (NLJ)** — avoid, degenerates to Cartesian product without B-tree indexes. Engine uses table statistics to auto-select; use hints to override when statistics are stale or unavailable. Ideal state: BHJ used wherever one table is small enough; no NLJ in production plans. | EXPLAIN plan for join type used; monitor shuffle and spill metrics; verify row count after join doesn't multiply unexpectedly (sign of Cartesian product) | DS_DIST_NONE (no redistribution), DS_DIST_ALL (broadcast small table), DS_DIST_BOTH (both sides shuffle — most expensive). [Redshift join strategies](https://docs.aws.amazon.com/redshift/latest/dg/c-the-query-plan.html) | `broadcast(df)`, `spark.sql.autoBroadcastJoinThreshold`. Hints: `/*+ BROADCAST(t) */`, `/*+ MERGE(t) */`. AQE auto-selects strategy at runtime. [Join hints](https://spark.apache.org/docs/3.0.0/sql-ref-syntax-qry-select-hints.html) \| [AQE](https://spark.apache.org/docs/latest/sql-performance-tuning.html#adaptive-query-execution) | Presto/Trino-based; `set join_distribution_type = 'BROADCAST'` session property. [Athena query tuning](https://docs.aws.amazon.com/athena/latest/ug/performance-tuning.html) | Spark-based; same broadcast hints apply. `from pyspark.sql.functions import broadcast` |
| **4. Query & Compute Execution** | Data Movement & Shuffle | Shuffle (data redistribution across nodes) is the most expensive operation in distributed compute — serialization, network transfer, deserialization. Minimize by: broadcasting small tables, pre-partitioning/bucketing on join keys at write time, co-locating related data, using AQE to reduce post-shuffle partition count. Ideal state: zero shuffle operations in the execution plan; data is co-located at rest. | Spark UI shuffle read/write bytes per stage; Redshift EXPLAIN for redistribution type (INNER_DIST, OUTER_DIST, BOTH_DIST); network I/O metrics | EXPLAIN shows: INNER_DIST, OUTER_DIST, BOTH_DIST. Design dist keys to eliminate redistribution. [Redshift redistribution](https://docs.aws.amazon.com/redshift/latest/dg/c-the-query-plan.html) | `spark.sql.shuffle.partitions` controls post-shuffle partition count. AQE coalesces small partitions automatically. `cache()` intermediate results to avoid re-shuffle. [AQE docs](https://spark.apache.org/docs/latest/sql-performance-tuning.html#adaptive-query-execution) | No explicit shuffle control; optimize through partitioning and avoiding cross-partition joins | Same as PySpark (Spark-based); set `spark.sql.shuffle.partitions` in Glue job parameters |
| **4. Query & Compute Execution** | Data Skew | Uneven data distribution causes some tasks to run much longer than others (straggler tasks), prolonging overall job time. Common causes: high-frequency values on join/group-by keys, null join keys, uneven partition sizes. Solutions: **salting** (add random prefix to hot keys), **separate processing** of skewed keys (e.g., null values handled separately), **AQE skew join** optimization. Ideal state: all tasks complete in roughly equal time; max task duration < 2x median. | Spark UI task duration variance per stage; Redshift `STL_ALERT_EVENT_LOG` for skew warnings; check max/min/avg partition sizes; plot task duration histogram | `STL_ALERT_EVENT_LOG` for skew alerts. Consider EVEN distribution for heavily skewed tables. [Redshift table distribution](https://docs.aws.amazon.com/redshift/latest/dg/c_choosing_dist_style.html) | AQE skew join: `spark.sql.adaptive.skewJoin.enabled=true`. Manual: salt hot keys, process nulls separately before joining. [AQE skew join](https://spark.apache.org/docs/latest/sql-performance-tuning.html#skew-join-optimization) \| [Salting technique](https://towardsdatascience.com/salting-your-spark-to-avoid-skew-aa987185a1d2) | Partition data to distribute load evenly; avoid joining on low-cardinality or nullable columns | Same as PySpark (Spark-based); configure AQE properties in Glue job `--conf` parameters |
| **4. Query & Compute Execution** | Resource Allocation (Compute / Memory) | Insufficient memory causes disk spills during sort, aggregation, and join operations. Disk spills force multi-pass algorithms that can cause exponential performance degradation. Right-size compute based on query memory requirements, not average load. Profile peak memory usage. Ideal state: no spills to disk; all sort and aggregation operations complete in memory; cluster utilization is consistent without queuing. | Spark UI executor memory metrics and spill-to-disk indicators; Redshift `STL_ALERT_EVENT_LOG` for memory warnings; cloud provider dashboards for cluster-level memory utilization | WLM (Workload Management) queues control memory per query class. `STL_ALERT_EVENT_LOG` for spill events. [Redshift WLM](https://docs.aws.amazon.com/redshift/latest/dg/c_workload_mngmt_classification.html) | `spark.executor.memory`, `spark.driver.memory`, `spark.memory.fraction`. Spark UI for spill-to-disk metrics. [Spark memory tuning](https://spark.apache.org/docs/latest/tuning.html#memory-management-overview) \| Cradle: container size configuration | Serverless — memory managed automatically. Split large queries or use CTAS to break them up. [Athena performance tuning](https://docs.aws.amazon.com/athena/latest/ug/performance-tuning.html) | Worker types: G.1X (16 GB), G.2X (32 GB), G.4X (64 GB), G.8X (128 GB). Set `--conf spark.executor.memory` in job parameters. [Glue worker types](https://docs.aws.amazon.com/glue/latest/dg/add-job.html) |
| **4. Query & Compute Execution** | Approximate Computation | For high-cardinality COUNT DISTINCT operations where exact results are not required (e.g., unique user counts as model inputs, dashboard metrics), use probabilistic algorithms. **HyperLogLog (HLL)** for distinct counts (~1–2% error); **KLL sketches** for quantiles. Orders of magnitude faster and more memory-efficient than exact algorithms. Ideal state: approximate functions are used where error tolerance permits; exact distinct counts are reserved only for billing or compliance use cases. | Compare approximate vs exact results to confirm error is within acceptable bounds; measure query speedup; document error tolerance in the data contract | `APPROXIMATE COUNT(DISTINCT col)` uses HLL internally. [Redshift approximate functions](https://docs.aws.amazon.com/redshift/latest/dg/r_APPROXIMATE_COUNT_DISTINCT.html) | `approx_count_distinct(col, rsd)`. For pre-aggregated HLL sketches, use `max()` followed by `sum()` rather than `count(distinct col)`. [Spark approx_count_distinct](https://spark.apache.org/docs/latest/api/python/reference/pyspark.sql/api/pyspark.sql.functions.approx_count_distinct.html) \| [HyperLogLog explainer](https://engineering.fb.com/2018/12/13/data-infrastructure/hyperloglog/) | `approx_distinct(col)` — Presto native HLL. [Presto aggregate functions](https://prestodb.io/docs/current/functions/aggregate.html#approx_distinct) | Same as PySpark (Spark-based); `approx_count_distinct` in Spark SQL |
| **4. Query & Compute Execution** | SQL Construct Choices | SQL is declarative — semantically equivalent queries can produce very different execution plans. Key choices: **DISTINCT vs GROUP BY** (GROUP BY is often more optimizer-friendly); **UNION ALL vs UNION** (UNION deduplicates, adding an implicit sort/hash step — use UNION ALL when duplicates are impossible); **CTE vs subquery** (behavior varies widely — some engines inline CTEs, others materialize them as temp tables); **IN vs EXISTS vs JOIN** for semi-joins; **LIKE vs REGEXP vs IN** for string filtering. Ideal state: SQL constructs are chosen with knowledge of how the target engine parses them, not just semantic equivalence. | Compare EXPLAIN plans for equivalent query rewrites; measure actual execution time for DISTINCT vs GROUP BY, UNION vs UNION ALL; check if CTE is inlined or materialized | CTEs not materialized by default — optimizer inlines them. Use temp tables to force materialization. [Redshift WITH clause](https://docs.aws.amazon.com/redshift/latest/dg/r_WITH_clause.html) | CTEs in Spark SQL are inlined; use `cache()` or temp views to force materialization. [Spark SQL CTEs](https://spark.apache.org/docs/latest/sql-ref-syntax-qry-select-cte.html) | Presto/Trino inlines CTEs by default. [Athena SQL reference](https://docs.aws.amazon.com/athena/latest/ug/select.html) | Spark SQL behavior; CTEs inlined — same as PySpark |
| **4. Query & Compute Execution** | Execution Plan Analysis | Always validate execution plans for non-trivial queries before production. EXPLAIN shows the optimizer's chosen strategy. EXPLAIN ANALYZE (or equivalent) shows actual execution metrics — rows processed, bytes read, spills, duration. Ideal state: estimated row counts match actual row counts (< 2x discrepancy), join strategies match intent, partition pruning is confirmed, and no unexpected full scans appear. | Compare estimated vs actual row counts (large discrepancy = stale stats); identify high-cost operators; verify no full scans on large tables; confirm expected join type is used | `EXPLAIN query_text` for physical plan. `STL_EXPLAIN` for stored plans post-execution. `SVL_QLOG` for execution stats. [Redshift EXPLAIN](https://docs.aws.amazon.com/redshift/latest/dg/r_EXPLAIN.html) | `df.explain(mode="extended")` or `df.explain(True)`. Spark UI → SQL tab for visual DAG with metrics per operator. [Spark explain](https://spark.apache.org/docs/latest/api/python/reference/pyspark.sql/api/pyspark.sql.DataFrame.explain.html) | `EXPLAIN` and `EXPLAIN ANALYZE` statements available. [Athena EXPLAIN](https://docs.aws.amazon.com/athena/latest/ug/athena-explain-statement.html) | `df.explain()` in code; output logged to CloudWatch. Spark UI available via Glue job monitoring. [Glue debugging](https://docs.aws.amazon.com/glue/latest/dg/monitor-debug-ui.html) |
| **5. Precomputation & Reuse** | Materialized Views | Pre-execute expensive query transformations and persist results. Refresh strategy options: **on-demand**, **incremental** (only processes new data — much cheaper), or **scheduled full refresh**. Staleness tolerance determines refresh frequency. Ideal state: expensive, stable aggregations are materialized and auto-refreshed incrementally; downstream queries transparently redirect to the view without modification. | Track view refresh duration and frequency; compare query latency with vs without materialized view; monitor staleness (time since last refresh); track automated vs manual refresh rate | Materialized views with `AUTO REFRESH` and incremental refresh. `SVV_MV_INFO` for refresh status. [Redshift materialized views](https://docs.aws.amazon.com/redshift/latest/dg/materialized-view-overview.html) | No native materialized views; write pre-computed DataFrames to Delta tables; schedule refresh via Cradle pipeline dependencies | No native materialized views; use CTAS as equivalent and refresh via scheduled Glue jobs. [Athena CTAS](https://docs.aws.amazon.com/athena/latest/ug/ctas.html) | ETL jobs write pre-computed results to S3; schedule via Glue Workflows or EventBridge. [Glue Workflows](https://docs.aws.amazon.com/glue/latest/dg/workflows_overview.html) |
| **5. Precomputation & Reuse** | Engine-Level Caching | Engines cache at multiple levels: **result cache** (exact same query returns instantly from cache); **data cache** (frequently accessed data blocks cached in fast storage); **metadata cache** (partition metadata cached to avoid catalog round-trips). Cache hit rates should be monitored. Result caches invalidate on data changes. Most effective for repeated, parameterized dashboard queries. Ideal state: high-frequency dashboard queries achieve near-100% result cache hit rate. | Compare query latency: first run (cache miss) vs subsequent runs (cache hit); monitor cache hit ratio in system metrics; verify cache invalidates correctly after data updates | Result cache enabled by default; `SET enable_result_cache_for_session = ON`. [Redshift result cache](https://docs.aws.amazon.com/redshift/latest/dg/r_enable_result_cache_for_session.html) | `df.cache()` / `df.persist(StorageLevel.MEMORY_AND_DISK)`. View cached DataFrames in Spark UI Storage tab. [Spark caching](https://spark.apache.org/docs/latest/rdd-programming-guide.html#rdd-persistence) | No result cache; responses always freshly computed. Consider a caching layer (e.g., Redshift Serverless) for repeated dashboard queries | Spark caching available (`df.cache()`); Glue job bookmarks for incremental reuse to avoid reprocessing. [Glue bookmarks](https://docs.aws.amazon.com/glue/latest/dg/monitor-continuations.html) |
| **5. Precomputation & Reuse** | Pre-aggregated Tables | Compute common aggregations once (daily totals, cohort metrics, dimension rollups) and store as purpose-built tables. Design a dependency chain: raw → daily → weekly → monthly. Use append-only patterns for idempotent rebuilds. Ideal state: no query scans the raw fact table for a metric that could have been pre-aggregated; downstream consumers always query the smallest available table that meets their granularity. | Compare bytes scanned: query on raw fact table vs pre-aggregated table; measure ETL refresh time for each aggregation tier; audit downstream query patterns to find missed pre-aggregation opportunities | Pre-aggregated tables as regular Redshift tables; refresh via scheduled queries or ETL pipeline. [Redshift scheduled queries](https://docs.aws.amazon.com/redshift/latest/mgmt/query-editor-schedule-query.html) | Write aggregated DataFrames to Parquet/Delta; orchestrate dependency chain via Cradle pipeline | CTAS to create aggregated tables in S3; query the aggregate table for dashboards. [Athena CTAS](https://docs.aws.amazon.com/athena/latest/ug/ctas.html) | Glue ETL jobs write rollup tables; Glue Workflows for dependency management. [Glue Workflows](https://docs.aws.amazon.com/glue/latest/dg/workflows_overview.html) |
| **6. Operations & Lifecycle** | Table Maintenance (Vacuum / Compaction / Stats) | Over time: delete/update operations leave dead rows (Redshift), small files accumulate (S3-based engines), statistics become stale. Regular maintenance: **vacuum** removes dead rows and reclaims space; **compaction** merges small files into target size; **ANALYZE** updates statistics. Skipping maintenance causes gradual, hard-to-diagnose query performance degradation. Ideal state: maintenance runs automatically on a schedule; no table has dead row ratio > 10%, file count is stable, and statistics are < 24 hours old. | Check dead row ratio (`SVV_TABLE_INFO` in Redshift); count files per partition; compare statistics age vs last data modification timestamp | `VACUUM table` (reclaims space, re-sorts data); `ANALYZE table` (updates statistics). `SVV_TABLE_INFO` for bloat. Auto-vacuum runs in background. [Redshift VACUUM](https://docs.aws.amazon.com/redshift/latest/dg/r_VACUUM_command.html) | Delta: `OPTIMIZE table` for file compaction; `VACUUM table RETAIN N HOURS` for old file cleanup. [Delta maintenance](https://docs.delta.io/latest/delta-utility.html) | Iceberg: `OPTIMIZE table REWRITE DATA` for compaction; `VACUUM table` to remove expired snapshots. [Iceberg maintenance on Athena](https://docs.aws.amazon.com/athena/latest/ug/querying-iceberg-data-optimization.html) | Iceberg/Delta compaction via Glue ETL jobs; schedule as periodic maintenance with Glue Triggers |
| **6. Operations & Lifecycle** | Data Retention & Archival | Define retention tiers per table: **hot** (recent, frequently queried — primary storage), **warm** (less frequent — cheaper storage tier), **cold** (archived or deleted per compliance policy). For open table formats, snapshot retention must be managed explicitly — uncleaned snapshots cause storage bloat (snapshot files accumulate even though the underlying data is "immutable by convention"). Ideal state: retention policies are defined, automated, and enforced per table; snapshot count is bounded. | Monitor partition storage size growth over time; track snapshot count for open table format tables; verify S3 Lifecycle policies are applied to correct prefixes | No automatic archival; use `UNLOAD` to S3 + `DROP TABLE` for old data. [Redshift UNLOAD](https://docs.aws.amazon.com/redshift/latest/dg/r_UNLOAD.html) | Delta: `VACUUM table RETAIN N HOURS`. Configure `delta.logRetentionDuration` table property. [Delta vacuum](https://docs.delta.io/latest/delta-utility.html#remove-files-no-longer-referenced-by-a-delta-table) | Iceberg: `ALTER TABLE ... SET TBLPROPERTIES ('history.expire.min-snapshots-to-keep'='5')`. S3 Lifecycle rules for data files. [Iceberg table properties](https://iceberg.apache.org/docs/latest/configuration/) | S3 Lifecycle policies for underlying data files; Glue ETL job to expire Iceberg snapshots on schedule. [S3 Lifecycle](https://docs.aws.amazon.com/AmazonS3/latest/userguide/object-lifecycle-mgmt.html) |
| **6. Operations & Lifecycle** | Database-Level Operations | Protect data through automated backups and point-in-time recovery. Test restoration procedures — an untested backup is not a backup. Snapshot before major schema changes or bulk loads. Validate data integrity with row count reconciliation between source and target systems. Ideal state: automated backups run on schedule, restoration has been tested, and any bulk operation is preceded by a snapshot. | Verify backup schedule and retention period; test restore procedure periodically; validate row counts after bulk loads; check backup storage costs | Automated snapshots every 8 hours; manual snapshots before migrations. `RESTORE` from snapshot. [Redshift backups](https://docs.aws.amazon.com/redshift/latest/mgmt/working-with-snapshots.html) | Delta table history provides time travel (audit, not backup). For backups, copy Parquet files to a separate S3 path/bucket before major operations. `DESCRIBE HISTORY table` for audit trail. | S3 versioning as backup mechanism; Iceberg time travel for point-in-time queries. [S3 versioning](https://docs.aws.amazon.com/AmazonS3/latest/userguide/Versioning.html) | Glue Data Catalog export for metadata backup; S3 replication for data backup. [Glue Catalog backup](https://docs.aws.amazon.com/glue/latest/dg/backup-restore-glue-catalog.html) |
| **6. Operations & Lifecycle** | Cluster & Infrastructure Operations | Plan managed upgrades, patches, and scaling for low-traffic windows. Monitor cluster health metrics proactively — reactive monitoring means users already noticed. Rightsize continuously: over-provisioning wastes money; under-provisioning causes queue buildup or failures. Ideal state: cluster utilization stays between 50–70%, upgrade windows are pre-scheduled, and scaling is automated based on utilization thresholds. | Monitor CPU/memory utilization trends; track query queue depth over time; review upgrade changelog for breaking changes before applying; compare pre/post-upgrade query performance | Classic or elastic cluster resize; scheduled maintenance windows. RA3 nodes with managed storage auto-scale storage independently of compute. [Redshift cluster management](https://docs.aws.amazon.com/redshift/latest/mgmt/working-with-clusters.html) \| [Redshift Serverless](https://docs.aws.amazon.com/redshift/latest/mgmt/serverless-overview.html) | Cradle manages cluster lifecycle; configure cluster size and instance type in job definition. Monitor via Cradle console. | Serverless — no cluster to manage. Manage via service quotas and per-workgroup concurrency limits. [Athena service quotas](https://docs.aws.amazon.com/athena/latest/ug/service-limits.html) | Serverless; scale by adjusting DPU per job. Auto Scaling available for Spark streaming jobs. [Glue DPU management](https://docs.aws.amazon.com/glue/latest/dg/add-job.html) |
| **7. Observability** | Resource & Query Metrics | Track two dimensions: **(1) Resource-level** — CPU, memory, disk I/O, network (identifies infrastructure bottlenecks); **(2) Query-level** — bytes scanned, shuffle read/write, spill to disk, query duration (identifies inefficient SQL). Ideal state: metrics are captured for every query, dashboarded by team/user, and used to detect regressions before users report them. | Query engine system tables and history logs; cloud monitoring dashboards for cluster-level metrics; Spark UI for job-level breakdown | `SVL_QUERY_METRICS`, `STL_QUERY`, `STV_EXEC_STATE`. CloudWatch for cluster-level metrics. [Redshift system tables](https://docs.aws.amazon.com/redshift/latest/dg/c_intro_system_tables.html) | Spark UI (port 4040) — Stages, Tasks, Executors tabs. Spark History Server for completed jobs. [Spark UI](https://spark.apache.org/docs/latest/web-ui.html) | CloudWatch Metrics: `BytesScanned`, `QueryExecutionTime`, `QueryQueueTime`. Query execution detail in Athena console history. [Athena CloudWatch metrics](https://docs.aws.amazon.com/athena/latest/ug/query-metrics-viewing.html) | CloudWatch for Glue job metrics (bytes read/written, shuffle bytes, memory). Glue job run detail in AWS console. [Glue CloudWatch metrics](https://docs.aws.amazon.com/glue/latest/dg/monitoring-awsglue-with-cloudwatch-metrics.html) |
| **7. Observability** | Adoption & Throughput Metrics | Track system-level usage: active users, query volume, concurrency, queue depth, SLA compliance rate. Rising query volume without proportional resource scaling causes latency degradation. Adoption metrics inform capacity planning and identify underutilized clusters. Ideal state: query volume trends are visible, SLA compliance is tracked per team or workgroup, and capacity scaling decisions are driven by trend data not incidents. | Query system audit logs; review WLM queue statistics; track query success/failure rates; plot query volume over time | `STL_QUERY` for query history; `STV_WLM_QUERY_STATE` for live queue state; `STL_WLM_QUERY` for WLM per-query metrics. [Redshift WLM monitoring](https://docs.aws.amazon.com/redshift/latest/dg/cm-c-wlm-query-monitoring-rules.html) | Spark History Server for job-level throughput; Cradle console for pipeline-level metrics and SLA tracking | Athena Workgroups with per-workgroup query count and bytes scanned metrics. [Athena workgroups](https://docs.aws.amazon.com/athena/latest/ug/manage-queries-control-costs-with-workgroups.html) | AWS Cost Explorer for DPU usage trends; Glue job run history in console; CloudWatch for job counts and durations. [Glue monitoring](https://docs.aws.amazon.com/glue/latest/dg/monitor-glue.html) |
| **7. Observability** | Execution Plan Inspection | Make execution plan review a standard step in query development — not just debugging. EXPLAIN shows the optimizer's strategy. EXPLAIN ANALYZE (or equivalent) shows actual runtime metrics. Large estimated vs actual row count discrepancy signals stale statistics. Ideal state: every production query has a reviewed EXPLAIN plan, estimated rows are within 2x of actual, and plan review is part of the code review checklist for data pipelines. | Run EXPLAIN before deploying any non-trivial query; compare estimated vs actual row counts; verify expected join type appears in plan; confirm partition pruning is active | `EXPLAIN query_text` — full physical plan. `STL_EXPLAIN` for stored plans post-execution. [Redshift EXPLAIN](https://docs.aws.amazon.com/redshift/latest/dg/r_EXPLAIN.html) | `df.explain(mode='cost')` for cost-based plan. Spark UI SQL tab shows physical plan with per-operator row counts. [Spark explain modes](https://spark.apache.org/docs/latest/api/python/reference/pyspark.sql/api/pyspark.sql.DataFrame.explain.html) | `EXPLAIN` and `EXPLAIN ANALYZE` statements. [Athena EXPLAIN](https://docs.aws.amazon.com/athena/latest/ug/athena-explain-statement.html) | `df.explain()` in Glue PySpark script; output to CloudWatch logs. [Glue debugging](https://docs.aws.amazon.com/glue/latest/dg/monitor-debug-ui.html) |
| **7. Observability** | Alerts & Automation | Define SLA thresholds for query duration, resource utilization, job failure rate, and data freshness lag. Alert on **leading indicators** (queue depth rising, memory pressure building) not just lagging ones (SLA already breached). Automate remediation where safe: kill runaway queries, retry failed jobs, auto-scale. Ideal state: no human monitors dashboards for routine issues; all known failure modes have automated detection and at least a pager alert with a runbook link. | Verify alerts fire correctly in staging before enabling in production; review alert fatigue — high false positive rate means alerts are ignored; confirm each alert has a runbook | Query Monitoring Rules (QMR) in WLM auto-cancel or hop long-running queries. CloudWatch alarms for cluster-level metrics. [Redshift QMR](https://docs.aws.amazon.com/redshift/latest/dg/cm-c-wlm-query-monitoring-rules.html) | Cradle pipeline alerts on job failure; CloudWatch alarms on Spark job metrics; custom `SparkListener` for programmatic in-job monitoring | CloudWatch alarms on `QueryExecutionTime` and `BytesScanned`; Athena Workgroup controls auto-cancel queries exceeding byte scan limit. [Athena workgroup controls](https://docs.aws.amazon.com/athena/latest/ug/workgroups-setting-control-limits-cloudwatch.html) | Glue job triggers on failure/completion; CloudWatch alarms; SNS notifications for pipeline failures. [Glue job notifications](https://docs.aws.amazon.com/glue/latest/dg/glue-notifications.html) |
